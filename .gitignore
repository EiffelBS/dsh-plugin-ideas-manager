node_modules/
*.log

# Local runtime artifacts — never commit these
.migration-review/
.test-*-url.txt
backups/
*-out.txt
# Tool transcripts written while iterating (typecheck / vitest / bundler)
tc*.txt
r[0-9]*.txt
test-*.txt

